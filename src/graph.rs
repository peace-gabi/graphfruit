use crate::node::{Node, NodeId, NodeInfo};
use crate::relation::{Relation, RelationId, RelationInfo};

// TODO: Implement Graph

#[cfg(test)]
mod tests {
    use super::*;

    // Add functions inside this module with the test attribute to
    // add additional tests.
    #[test]
    fn works_correctly() {
        assert_eq!(1, 1);
    }
}
